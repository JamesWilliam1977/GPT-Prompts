interface:
  display_name: "GPT Prompts"
  short_description: "Repo-specific patterns and workflows for GPT-Prompts"
  default_prompt: "Use the GPT-Prompts repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true